# Lumen Kulo

## How to install

```
composer install
php artisan jwt:secret
php artisan migrate
ln -s {path_to/storage/app/files} {path_to/public/files}
```

## How to run dev

```
php artisan serve
```